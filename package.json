{
  "name": "jpmorgan-exercise",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/FMGordillo/jpmorgan-selection-react",
  "dependencies": {
    "@testing-library/jest-dom": "^5.5.0",
    "@testing-library/react": "^10.0.2",
    "@testing-library/user-event": "^10.0.1",
    "axios": "^0.21.1",
    "concurrently": "^5.1.0",
    "json-server": "^0.16.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "reactbulma": "^3.0.4",
    "styled-components": "^5.1.0"
  },
  "scripts": {
    "run-db": "json-server --watch db.json --port 3001 --no-cors",
    "start": "concurrently \"npm run run-db\" \"react-scripts start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
